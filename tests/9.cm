/*Line 8 produces invalid type error, line 25 produces return type mismatch   */

int array[5];

int findMax(int b[], int start, int end) {
  int i; 
  int maxValue; 
  void q;
  int index;
  bool result;

  index = q + 1;
  index = start;
  maxValue = b[start];
  i = start + 1;
  while (i < end) {
    if (b[i] > maxValue) {
       maxValue = b[i];
       index = i; 
    }
    i = i + 1;
  
  } 

  return result; 
}

void main(void){
  int x;
  int y;
  int c;
  x=1;
  y=2;
  c=3;

  findMax(y,c,x);
}
